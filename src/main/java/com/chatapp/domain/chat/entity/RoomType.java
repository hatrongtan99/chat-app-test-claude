package com.chatapp.domain.chat.entity;

/**
 * The type of a chat room.
 */
public enum RoomType {
    /**
     * A public room visible to all users.
     */
    PUBLIC,
    /**
     * A private room visible only to its members.
     */
    PRIVATE
}
